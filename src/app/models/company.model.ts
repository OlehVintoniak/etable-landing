export interface CompanyInfo {
  name: string;
  tagline: string;
  ctaLabel: string;
  ctaTarget: string;
}

export interface ContactDetail {
  email: string;
  phone: string;
}

export interface SocialMediaLink {
  url: string;
  label: string;
}
